name: main
description: A sample command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  http: ^0.13.4 # For HTTP
  #path: ^1.8.0

# Only needed during development, not for release
dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
